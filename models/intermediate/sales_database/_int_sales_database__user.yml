models:
  - name: int_sales_database__user
    description: '{{ doc("int_sales_database__user") }}'
    columns:
      - name: user_id
        description: "Primary key, unique identifier for each uers."
        tests:
            - unique
            - not_null